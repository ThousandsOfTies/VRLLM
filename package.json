{
  "name": "vroid-llm-avatar",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "build:vscode": "vite build --config vite.config.vscode.js",
    "package:vscode": "npm run build:vscode && cd vscode-extension && npx vsce package --no-dependencies"
  },
  "dependencies": {
    "@pixiv/three-vrm": "^3.1.3",
    "@pixiv/three-vrm-animation": "^3.1.3",
    "three": "^0.170.0"
  },
  "devDependencies": {
    "vite": "^6.0.0"
  }
}
